def sao_multiplicaveis(m1, m2):
    matriz_multip = []
    linha = len(m1)
    coluna = len(m1[0])
    linha2 = len(m2)
    coluna2 = len(m2[0])
    if coluna == linha2:
        for i in range(linha):
            for j in range(coluna):
                matriz_multip
        return True
    else:
        return False
